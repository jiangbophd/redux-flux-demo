import React from 'react';

import { HashRouter } from 'react-router-dom'
import App from './containers/app/app';

const Root = () => (
			<HashRouter>
					<App />
			</HashRouter>
);

export default Root;